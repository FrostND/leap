% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_session_lag.R
\name{add_session_lag}
\alias{add_session_lag}
\title{Calculates the number of days between psychotherapy sessions.}
\usage{
add_session_lag(data)
}
\arguments{
\item{data}{A data frame containing longitudinal psychotherapy records.}

\item{client}{Character string specifying the name of the client identifier variable.}

\item{date}{Character string specifying the name of the session date variable. The
variable must be of class \code{Date}.}
}
\value{
A data frame with an additional variable, \code{session_lag}, representing the number of days
since the previous session for each client.
}
\description{
Calculates the number of days elapsed between consecutive psychotherapy
sessions within each client. The first session for each client is assigned
a lag value of 0. Records should be sorted by client and session date prior
to calling this function.
}
\details{
Session lags are calculated separately for each client using the difference
between consecutive session dates. Incorrect results may occur if records
are not ordered chronologically within client. Use \code{\link[=order_sessions]{order_sessions()}} before
calculating session lags.
}
\examples{
# calculate session lags
sorted <- add_session_lag(data = sorted, client = "client_id", date = "session_date")

# with native R pipe
sort_sessions(data = "unsorted", "client = "client_id", date = "session_id") |>
   add_session_lag(client = "client_id", date = "session_id")

# with maggritr pipe
sort_sessions(data = "unsorted", "client = "client_id", date = "session_id") \%>\%
   add_session_lag(client = "client_id", date = "session_id")
}
