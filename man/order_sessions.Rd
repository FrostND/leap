% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/order_sessions.R
\name{order_sessions}
\alias{order_sessions}
\title{Order longitudinal session records}
\usage{
order_sessions(data, by = c("date", "episode"))
}
\arguments{
\item{data}{A data frame containing session-level treatment records.}

\item{by}{Character string specifying how records should be ordered. Must be
either \code{"date"} or \code{"episode"}. When \code{by = "date"}, records are ordered by
\code{client_id} and \code{session_date}. When \code{by = "episode"}, records are ordered
by \code{client_id}, \code{episode_id}, and \code{episode_session}. Defaults to \code{"date"}.}
}
\value{
A data frame containing the original observations in the requested order.
Row names are reset after sorting.
}
\description{
Orders longitudinal treatment records either chronologically by client and
session date or structurally by client, treatment episode, and session
within episode.
}
\details{
Proper ordering is important before calculating session lags, identifying
treatment episodes, or performing episode-level analyses.

When \code{by = "date"}, the function requires the standardized variables
\code{client_id} and \code{session_date}. This option is intended primarily for raw
data before treatment episodes have been identified.

When \code{by = "episode"}, the function requires \code{client_id}, \code{episode_id}, and
\code{episode_session}. This option is intended for data that have already been
processed using episodeR.

Missing values in any variable used for sorting result in an error because
they prevent unambiguous ordering of session records.
}
\examples{
\dontrun{
# Sort raw records chronologically within clients
data <- sort_sessions(
  data = treatment_data,
  by = "date"
)

# Sort prepared data by treatment episode and session
data <- sort_sessions(
  data = treatment_data,
  by = "episode"
)

# Compatible with the native R pipe
treatment_data |>
  sort_sessions(by = "date") |>
  add_session_lag()
}

}
\seealso{
\code{\link[=cols_standard]{cols_standard()}},
\code{\link[=add_session_lag]{add_session_lag()}},
\code{\link[=add_episode_id]{add_episode_id()}},
\code{\link[=add_episode_session]{add_episode_session()}}
}
