% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_episode_change.R
\name{plot_episode_change}
\alias{plot_episode_change}
\title{Plot change across treatment episodes}
\usage{
plot_episode_change(
  data,
  cohort = c("all", "multiple"),
  max_episodes = NULL,
  show_individuals = TRUE,
  y_lims = NULL
)
}
\arguments{
\item{data}{A data frame containing episode-level summaries produced by
\code{\link[=episode_slopes]{episode_slopes()}}. The data must contain \code{client_id}, \code{episode_id},
\code{n_episodes}, and \code{change}.}

\item{cohort}{Character string specifying the client cohort to display.
Must be one of \code{"all"} or \code{"multiple"}. \code{"all"} includes all clients,
whereas \code{"multiple"} restricts the plot to clients with more than one
treatment episode. Defaults to \code{"all"}.}

\item{max_episodes}{Optional numeric value specifying the highest treatment
episode to display. For example, \code{max_episodes = 3} displays Episodes 1
through 3. Clients with additional episodes may still contribute
observations from earlier episodes. Defaults to \code{NULL}.}

\item{show_individuals}{Logical indicating whether individual client change
profiles and observations should be displayed. Defaults to \code{TRUE}.}

\item{y_lims}{Optional numeric vector of length two specifying the displayed
y-axis limits. Defaults to \code{NULL}.}
}
\value{
A \code{ggplot} object showing change scores across treatment episodes.
}
\description{
Plots episode-level change scores across successive treatment episodes.
Individual client profiles may be displayed in the background, while mean
change scores and 95\% confidence intervals for each episode number are
displayed in the foreground.
}
\details{
\code{plot_episode_change()} operates on episode-level data produced by
\code{\link[=episode_slopes]{episode_slopes()}}. Each row represents one treatment episode for a client,
and \code{change} represents the difference between the first and final outcome
observations within that episode.

The direction of change is determined when \code{\link[=episode_slopes]{episode_slopes()}} is called.
When episode slopes are calculated with \code{higher_is_better = TRUE}, positive
change scores indicate improvement. When \code{higher_is_better = FALSE}, the
direction of change is reversed so that positive values continue to
represent improvement.

For each treatment episode number, the function calculates the mean change
score and an approximate 95\% confidence interval:

\deqn{
\overline{\mathrm{Change}}_j
\mathbin{\pm}
1.96 \times \mathrm{SE}_j
}

where \(j\) denotes treatment episode number.

When \code{show_individuals = TRUE}, observations from the same client are
connected across episodes to show within-client patterns of change.

The composition of the sample may differ across episode numbers because
only clients who return for additional treatment contribute observations
to later episodes. Consequently, differences across episode numbers should
not necessarily be interpreted as within-client change.
}
\examples{
\dontrun{
# Calculate episode-level slopes and change scores
episodes <- episode_slopes(treatment_data)

# Plot change across all treatment episodes
plot_episode_change(data = episodes)

# Restrict to clients with multiple treatment episodes
plot_episode_change(data = episodes, cohort = "multiple")

# Display only the first three treatment episodes
plot_episode_change(data = episodes, cohort = "multiple", max_episodes = 3)

# Hide individual client profiles
plot_episode_change(data = episodes, show_individuals = FALSE)

# Reverse the direction of change for an outcome where lower is better
episodes <- episode_slopes(treatment_data, higher_is_better = FALSE)

plot_episode_change(episodes)
}

}
\seealso{
\code{\link[=episode_slopes]{episode_slopes()}},
\code{\link[=plot_episode_curves]{plot_episode_curves()}}
}
