% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_lme.R
\name{fit_lme}
\alias{fit_lme}
\title{Fit a multilevel treatment episode model}
\usage{
fit_lme(
  data,
  center = TRUE,
  cohort = c("all", "multiple"),
  model = c("null", "session", "episode", "full")
)
}
\arguments{
\item{data}{A data frame containing session-level treatment records. The
data must contain \code{client_id}, \code{episode_id}, \code{episode_session}, and
\code{outcome}.}

\item{center}{Logical indicating whether session and episode number should
be centered for interpretation. When \code{TRUE}, \code{episode_session} is centered
at Session 1 and \code{episode_id} is centered at Episode 1. Defaults to \code{TRUE}.}

\item{cohort}{Character string specifying the client cohort to analyze.
Must be one of \code{"all"} or \code{"multiple"}. \code{"all"} includes all clients,
whereas \code{"multiple"} restricts the analysis to clients with more than one
treatment episode. Defaults to \code{"all"}.}

\item{model}{Character string specifying the model to fit. Must be one of
\code{"null"}, \code{"session"}, \code{"episode"}, or \code{"full"}. Defaults to \code{"null"}.}
}
\value{
A fitted \code{lmerMod} object returned by \code{\link[lme4:lmer]{lme4::lmer()}}.
}
\description{
Fits a linear mixed-effects model for session-level outcomes across repeated
treatment episodes. Models can estimate change within treatment episodes,
differences across successive episodes, and whether rates of within-episode
change vary across episode number.
}
\details{
Sessions are nested within treatment episodes, which are nested within
clients. Four model specifications are available:

\describe{
\item{\code{"null"}}{Fits an unconditional random-intercept model with
treatment episodes nested within clients.}
\item{\code{"session"}}{Adds session within episode as a fixed effect and
allows session trajectories to vary across clients and treatment
episodes.}
\item{\code{"episode"}}{Adds treatment episode number to the session model,
allowing average outcome levels to differ across successive episodes.}
\item{\code{"full"}}{Adds the interaction between session within episode and
episode number, allowing rates of within-episode change to differ across
successive episodes.}
}

For models containing a session effect, the random-effects structure is:

\deqn{
(\mathrm{Session} \mid \mathrm{client} / \mathrm{episode})
}

which expands to random intercepts and random slopes for session at both
the client and client-by-episode levels.

When \code{center = TRUE}, Session 1 and Episode 1 are coded as zero.
Consequently, the intercept represents the expected outcome at the first
session of the first treatment episode. In the \code{"full"} model, the session
coefficient represents the expected rate of change during Episode 1, and
the session-by-episode interaction represents change in the session
trajectory across successive treatment episodes.
}
\examples{
\dontrun{
# Unconditional model
m0 <- fit_lme(data = treatment_data, model = "null")

# Model within-episode change
m1 <- fit_lme(data = treatment_data, model = "session")

# Full model among clients with multiple treatment episodes
m2 <- fit_lme(data = treatment_data,  cohort = "multiple", model = "full")

summary(m2)
}

}
\seealso{
\code{\link[=fit_sao]{fit_sao()}},
\code{\link[=fit_brms]{fit_brms()}},
\code{\link[lme4:lmer]{lme4::lmer()}}
}
