% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_episode_vars.R
\name{add_episode_vars}
\alias{add_episode_vars}
\title{Derive and build all treatment episode variables from raw longitudinal data.}
\usage{
add_episode_vars(data, delimiter = 90)
}
\arguments{
\item{data}{A data frame containing longitudinal psychotherapy records.}

\item{client}{Character string specifying the name of the client identifier
variable.}

\item{date}{Character string specifying the name of the session date
variable. The variable must be of class \code{Date}.}

\item{threshold}{Numeric value indicating the minimum number of days
required to define a new treatment episode. Defaults to 90.}
}
\value{
A data frame with the following additional variables:
\describe{
\item{session_lag}{Number of days since the previous session.}
\item{episode_id}{Treatment episode identifier.}
\item{episode_session}{Session number within treatment episode.}
}
}
\description{
First sorts psychotherapy session records, calculates session lags, identifies
distinct treatment episodes per client, and assigns session numbers within
episodes, tabulates total episodes for each individual.
}
\details{
This function provides a convenient wrapper around
\code{\link[=order_sessions]{order_sessions()}}, \code{\link[=add_session_lag]{add_session_lag()}}, \code{\link[=add_episode_id]{add_episode_id()}},
\code{\link[=add_episode_session]{add_episode_session()}}, and \code{\link[=add_episode_count]{add_episode_count()}} for users who wish to
prepare episode-level data in a single step.

Treatment episodes are identified when the elapsed time between two
consecutive sessions meets or exceeds the specified threshold. By
default, a gap of 90 or more days indicates the beginning of a new
treatment episode.
}
\examples{
# load package data
data("dat_unb")

# identify treatment episodes
df <- add_episode_vars(data = dat_unb, client = "client_id", date = "session_date")

# Use a custom episode threshold
df <- add_episode_vars(data = dat_unb, client = "client_id", date = "session_date", delimiter = 120)


}
