% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/episode_slopes.R
\name{episode_slopes}
\alias{episode_slopes}
\title{Estimate episode-specific slopes and pre-post change}
\usage{
episode_slopes(data, higher_is_better = TRUE)
}
\arguments{
\item{data}{A data frame containing psychotherapy session records.}

\item{higher_is_better}{Logical indicating whether larger outcome values
represent better functioning. When \code{TRUE} (default), change is calculated
as \code{post - pre} and the estimated slope is returned unchanged. When
\code{FALSE}, change is calculated as \code{pre - post} and the estimated slope is
multiplied by \code{-1}, so that positive values consistently indicate
improvement.}

\item{client}{Character string specifying the client identifier column.
Defaults to \code{"client_id"}.}

\item{episode}{Character string specifying the treatment episode identifier
column. Defaults to \code{"episode_id"}.}

\item{episode_session}{Character string specifying the session number within
each treatment episode. Defaults to \code{"episode_session"}.}

\item{outcome}{Character string specifying the outcome variable.
Defaults to \code{"outcome"}.}
}
\value{
A data frame containing one row per client treatment episode with
the following variables:

\describe{
\item{client_id}{Client identifier.}
\item{episode_id}{Treatment episode identifier.}
\item{n_sessions}{Number of sessions observed within the episode.}
\item{n_episodes}{Total number of treatment episodes attended by the
client.}
\item{pre}{Outcome score at the first observed session of the episode.}
\item{post}{Outcome score at the final observed session of the episode.}
\item{change}{Episode pre-post change score. Positive values indicate
improvement.}
\item{slope}{Estimated linear rate of outcome change per session.
Positive values indicate improvement.}
}
}
\description{
Estimates a linear growth rate and pre-post change score for each client
treatment episode. A separate ordinary least squares regression is fitted
within each episode to estimate change per session, while the pre-post
change score summarizes the total change between the first and final
observed sessions.
}
\details{
For each client treatment episode, the function estimates the linear model:

\deqn{
Y_{ij} = \beta_{0j} + \beta_{1j}(\mathrm{Session}_{ij}) + \varepsilon_{ij}
}

where \eqn{\beta_{1j}} represents the estimated episode-specific rate of
change per session.

The episode change score is calculated from the first and final observed
sessions. When \code{higher_is_better = TRUE},

\deqn{
\mathrm{Change}_j = Y_{j,\mathrm{final}} - Y_{j,\mathrm{initial}}
}

whereas setting \code{higher_is_better = FALSE} reverses the subtraction so that
positive values continue to indicate clinical improvement. Likewise, the
estimated slope is multiplied by \code{-1} when lower outcome values indicate
better functioning.

The change score represents the total observed improvement over an episode,
whereas the slope represents the estimated rate of improvement per session.
These quantities may differ substantially when episode lengths vary.

Episodes containing fewer than two sessions cannot support estimation of a
linear growth rate and trigger a warning. Episodes containing four or fewer
sessions are also flagged because their estimated slopes may be unstable.

This function assumes that treatment episodes have already been identified
using \code{\link[=add_episode_id]{add_episode_id()}} and that session numbers within episodes have been
assigned using \code{\link[=add_episode_session]{add_episode_session()}}.
}
\examples{
\dontrun{
episode_estimates <- episode_slopes(
  data = treatment_data,
  client = "client_id",
  episode = "episode_id",
  episode_session = "episode_session",
  outcome = "outcome"
)

head(episode_estimates)

# Outcome where lower scores indicate improvement
episode_estimates <- episode_slopes(
  data = treatment_data,
  outcome = "symptom_score",
  higher_is_better = FALSE
)
}

}
\seealso{
\code{\link[=add_episode_id]{add_episode_id()}},
\code{\link[=add_episode_session]{add_episode_session()}},
\code{\link[=describe_episodes]{describe_episodes()}},
\code{\link[=fit_sao]{fit_sao()}}
}
